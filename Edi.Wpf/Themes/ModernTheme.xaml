<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- Palette: Modern dark with neon magenta + cyan accent -->
    <SolidColorBrush x:Key="App.WindowBg"     Color="#0E1015"/>
    <SolidColorBrush x:Key="App.PanelBg"      Color="#171A22"/>
    <SolidColorBrush x:Key="App.PanelBgHi"    Color="#1F222C"/>
    <SolidColorBrush x:Key="App.InputBg"      Color="#11141B"/>
    <SolidColorBrush x:Key="App.Border"       Color="#272C36"/>
    <SolidColorBrush x:Key="App.BorderStrong" Color="#363C49"/>
    <SolidColorBrush x:Key="App.BorderFocus"  Color="#FF2D8C"/>
    <SolidColorBrush x:Key="App.Text"         Color="#E7E9EE"/>
    <SolidColorBrush x:Key="App.TextMuted"    Color="#C7CCD6"/>
    <SolidColorBrush x:Key="App.TextDim"      Color="#7C828F"/>
    <SolidColorBrush x:Key="App.Accent"       Color="#FF2D8C"/>
    <SolidColorBrush x:Key="App.AccentSoft"   Color="#3A1A2A"/>
    <SolidColorBrush x:Key="App.AccentDark"   Color="#C71F6E"/>
    <SolidColorBrush x:Key="App.AccentAlt"    Color="#00E5FF"/>
    <SolidColorBrush x:Key="App.Success"      Color="#00FFA3"/>
    <SolidColorBrush x:Key="App.RowAlt"       Color="#1B1E27"/>
    <SolidColorBrush x:Key="App.RowHover"     Color="#22202E"/>
    <SolidColorBrush x:Key="App.RowSelect"    Color="#3A1A2A"/>
    <SolidColorBrush x:Key="App.HeaderBg"     Color="#13161D"/>
    <SolidColorBrush x:Key="App.Disabled"     Color="#1A1D24"/>
    <SolidColorBrush x:Key="App.DisabledText" Color="#4B5260"/>

    <sys:Double x:Key="App.FontSize">12</sys:Double>
    <FontFamily x:Key="App.FontFamily">Segoe UI Variable Text, Segoe UI, Tahoma</FontFamily>

    <!-- ============ Tooltip ============ -->
    <Style TargetType="ToolTip">
        <Setter Property="Background"      Value="#0B0D12"/>
        <Setter Property="Foreground"      Value="{StaticResource App.Text}"/>
        <Setter Property="BorderBrush"     Value="{StaticResource App.Accent}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding"         Value="8,4"/>
        <Setter Property="FontFamily"      Value="{StaticResource App.FontFamily}"/>
        <Setter Property="FontSize"        Value="{StaticResource App.FontSize}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============ Label ============ -->
    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource App.TextMuted}"/>
        <Setter Property="FontFamily" Value="{StaticResource App.FontFamily}"/>
        <Setter Property="FontSize"   Value="{StaticResource App.FontSize}"/>
        <Setter Property="Padding"    Value="0,2,0,2"/>
    </Style>

    <!-- ============ CheckBox ============ -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource App.Text}"/>
        <Setter Property="FontFamily" Value="{StaticResource App.FontFamily}"/>
        <Setter Property="FontSize"   Value="{StaticResource App.FontSize}"/>
        <Setter Property="Cursor"     Value="Hand"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border x:Name="box" Grid.Column="0"
                                Width="14" Height="14"
                                Background="{StaticResource App.InputBg}"
                                BorderBrush="{StaticResource App.BorderStrong}"
                                BorderThickness="1" CornerRadius="2"
                                VerticalAlignment="Center" Margin="0,0,6,0">
                            <Path x:Name="check"
                                  Data="M 2,7 L 5,10 L 12,3"
                                  Stroke="{StaticResource App.Accent}"
                                  StrokeThickness="2"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  Visibility="Collapsed"/>
                        </Border>
                        <ContentPresenter Grid.Column="1"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource App.Accent}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource App.Accent}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource App.DisabledText}"/>
                            <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource App.Border}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============ TextBox ============ -->
    <Style TargetType="TextBox">
        <Setter Property="Foreground"      Value="{StaticResource App.Text}"/>
        <Setter Property="Background"      Value="{StaticResource App.InputBg}"/>
        <Setter Property="BorderBrush"     Value="{StaticResource App.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontFamily"      Value="{StaticResource App.FontFamily}"/>
        <Setter Property="FontSize"        Value="{StaticResource App.FontSize}"/>
        <Setter Property="SelectionBrush"  Value="{StaticResource App.AccentSoft}"/>
        <Setter Property="CaretBrush"      Value="{StaticResource App.Accent}"/>
        <Setter Property="Padding"         Value="6,2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3"
                            SnapsToDevicePixels="True" UseLayoutRounding="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Padding="{TemplateBinding Padding}"
                                      VerticalAlignment="Center"
                                      Focusable="False"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource App.BorderStrong}"/>
                        </Trigger>
                        <!-- Accent outline on hover; DynamicResource so it follows a dialog's recolored
                             accent (e.g. blue for Script Player). IsMouseOver is last so it wins when both apply -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource App.Accent}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource App.DisabledText}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============ ComboBox (Toggle + Popup) ============ -->
    <Style TargetType="{x:Type ToggleButton}" x:Key="ComboToggleStyle">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="ClickMode" Value="Press"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Grid Background="Transparent">
                        <Path HorizontalAlignment="Right" VerticalAlignment="Center"
                              Margin="0,0,8,0" Width="8" Height="5"
                              Data="M 0,0 L 8,0 L 4,5 Z"
                              Fill="{StaticResource App.TextMuted}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Foreground"      Value="{StaticResource App.Text}"/>
        <Setter Property="Background"      Value="{StaticResource App.InputBg}"/>
        <Setter Property="BorderBrush"     Value="{StaticResource App.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontFamily"      Value="{StaticResource App.FontFamily}"/>
        <Setter Property="FontSize"        Value="{StaticResource App.FontSize}"/>
        <Setter Property="Padding"         Value="6,2"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"/>
                        <ToggleButton Style="{StaticResource ComboToggleStyle}"
                                      Grid.ColumnSpan="2"
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                        <ContentPresenter x:Name="ContentSite"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"/>
                        <TextBox x:Name="PART_EditableTextBox"
                                 Style="{x:Null}"
                                 Template="{x:Null}"
                                 HorizontalAlignment="Left"
                                 VerticalAlignment="Center"
                                 Margin="{TemplateBinding Padding}"
                                 Focusable="True"
                                 Background="Transparent"
                                 Foreground="{StaticResource App.Text}"
                                 CaretBrush="{StaticResource App.Accent}"
                                 Visibility="Hidden"
                                 IsReadOnly="{TemplateBinding IsReadOnly}"/>
                        <Popup x:Name="Popup" Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Grid x:Name="DropDown"
                                  SnapsToDevicePixels="True"
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border x:Name="DropDownBorder"
                                        Background="{StaticResource App.PanelBgHi}"
                                        BorderBrush="{StaticResource App.Accent}"
                                        BorderThickness="1"
                                        CornerRadius="3"
                                        Margin="0,2,0,0"/>
                                <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                </ScrollViewer>
                            </Grid>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter Property="IsTabStop" Value="False"/>
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource App.Accent}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource App.BorderStrong}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource App.DisabledText}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{StaticResource App.Text}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="itemBorder"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="0"
                            SnapsToDevicePixels="True">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="itemBorder" Property="Background" Value="{StaticResource App.RowHover}"/>
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="itemBorder" Property="Background" Value="{StaticResource App.RowSelect}"/>
                            <Setter Property="Foreground" Value="{StaticResource App.Accent}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                <Setter Property="Background" Value="Transparent"/>
            </Trigger>
            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                <Setter Property="Background" Value="{StaticResource App.RowAlt}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============ Buttons (default + accent variant) ============ -->
    <Style TargetType="Button">
        <Setter Property="Foreground"      Value="{StaticResource App.Text}"/>
        <Setter Property="Background"      Value="{StaticResource App.PanelBgHi}"/>
        <Setter Property="BorderBrush"     Value="{StaticResource App.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding"         Value="2,1"/>
        <Setter Property="Cursor"          Value="Hand"/>
        <Setter Property="FontFamily"      Value="{StaticResource App.FontFamily}"/>
        <Setter Property="FontSize"        Value="{StaticResource App.FontSize}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource App.Accent}"/>
                            <Setter Property="Foreground" Value="{StaticResource App.Accent}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background"  Value="{StaticResource App.AccentSoft}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource App.AccentDark}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource App.Accent}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background"  Value="{StaticResource App.Disabled}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource App.Border}"/>
                            <Setter Property="Foreground" Value="{StaticResource App.DisabledText}"/>
                            <Setter Property="Cursor"     Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Accent button (used for primary actions like Play, Reconnect) -->
    <!-- Cyan-filled twin of AccentButton — same shape/states, App.AccentAlt color, so two
         filled buttons next to each other read as distinct actions instead of one button
         that "doesn't look clickable". Hover/press shades are inlined since there's no
         App.AccentAltDark resource. -->
    <Style x:Key="AccentAltButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background"  Value="{StaticResource App.AccentAlt}"/>
        <Setter Property="BorderBrush" Value="{StaticResource App.AccentAlt}"/>
        <Setter Property="Foreground"  Value="#001318"/>
        <Setter Property="FontWeight"  Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#4DECFF"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#4DECFF"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#00B5CC"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#00B5CC"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background"  Value="{StaticResource App.Disabled}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource App.Border}"/>
                            <Setter Property="Foreground" Value="{StaticResource App.DisabledText}"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background"  Value="{StaticResource App.Accent}"/>
        <Setter Property="BorderBrush" Value="{StaticResource App.Accent}"/>
        <Setter Property="Foreground"  Value="#FFFFFF"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#FF4D9F"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#FF4D9F"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource App.AccentDark}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource App.AccentDark}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background"  Value="{StaticResource App.Disabled}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource App.Border}"/>
                            <Setter Property="Foreground" Value="{StaticResource App.DisabledText}"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============ Slider ============ -->
    <Style TargetType="{x:Type Thumb}" x:Key="SliderThumbStyle">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Grid>
                        <Ellipse Width="14" Height="14" Fill="{StaticResource App.Accent}"/>
                        <Ellipse Width="6" Height="6" Fill="#FFFFFF"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Slider">
        <Setter Property="Foreground" Value="{StaticResource App.Accent}"/>
        <Setter Property="Background" Value="{StaticResource App.Border}"/>
        <Setter Property="Cursor"     Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid Height="22">
                        <Border CornerRadius="2"
                                Height="4"
                                VerticalAlignment="Center"
                                Background="{TemplateBinding Background}"/>
                        <Track x:Name="PART_Track">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="Slider.DecreaseLarge">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border CornerRadius="2" Height="4" Background="{StaticResource App.Accent}" VerticalAlignment="Center"/>
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="Slider.IncreaseLarge">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Background="Transparent"/>
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource SliderThumbStyle}"/>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============ ProgressBar ============ -->
    <Style TargetType="ProgressBar">
        <Setter Property="Foreground" Value="{StaticResource App.Accent}"/>
        <Setter Property="Background" Value="{StaticResource App.PanelBg}"/>
        <Setter Property="BorderBrush" Value="{StaticResource App.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- ============ ScrollBar (compact dark, themed) ============ -->
    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource App.BorderStrong}"/>
        <Setter Property="Width" Value="10"/>
        <Setter Property="MinWidth" Value="10"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="Transparent" SnapsToDevicePixels="True">
                        <Track x:Name="PART_Track" Orientation="{TemplateBinding Orientation}" IsDirectionReversed="True">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton x:Name="decBtn" Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False" IsTabStop="False"/>
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border x:Name="th" CornerRadius="4" Margin="2"
                                                    Background="{StaticResource App.BorderStrong}"/>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="th" Property="Background" Value="{StaticResource App.Accent}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton x:Name="incBtn" Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False" IsTabStop="False"/>
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
                            <Setter TargetName="decBtn" Property="Command" Value="ScrollBar.PageLeftCommand"/>
                            <Setter TargetName="incBtn" Property="Command" Value="ScrollBar.PageRightCommand"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="Auto"/>
                <Setter Property="MinWidth" Value="0"/>
                <Setter Property="Height" Value="10"/>
                <Setter Property="MinHeight" Value="10"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============ DataGrid ============ -->
    <Style TargetType="DataGrid">
        <Setter Property="Background"               Value="Transparent"/>
        <Setter Property="BorderBrush"              Value="{StaticResource App.Border}"/>
        <Setter Property="BorderThickness"          Value="1"/>
        <Setter Property="RowBackground"            Value="Transparent"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource App.RowAlt}"/>
        <Setter Property="GridLinesVisibility"      Value="None"/>
        <Setter Property="RowHeaderWidth"           Value="0"/>
        <Setter Property="HeadersVisibility"        Value="Column"/>
        <Setter Property="Foreground"               Value="{StaticResource App.Text}"/>
        <Setter Property="FontFamily"               Value="{StaticResource App.FontFamily}"/>
        <Setter Property="FontSize"                 Value="14"/>
        <Setter Property="CanUserResizeColumns"     Value="True"/>
        <Setter Property="CanUserResizeRows"        Value="False"/>
        <Setter Property="RowHeight"                Value="36"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background"      Value="{StaticResource App.HeaderBg}"/>
        <Setter Property="Foreground"      Value="{StaticResource App.TextMuted}"/>
        <Setter Property="FontWeight"      Value="SemiBold"/>
        <Setter Property="FontSize"        Value="12"/>
        <Setter Property="Padding"         Value="10,8"/>
        <Setter Property="BorderBrush"     Value="{StaticResource App.Border}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding"         Value="10,6"/>
        <Setter Property="Foreground"      Value="{StaticResource App.Text}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource App.RowSelect}"/>
                <Setter Property="Foreground" Value="{StaticResource App.Text}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource App.RowHover}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============ Dialog button styles (shared) ============ -->
    <Style x:Key="DialogButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Width"      Value="108"/>
        <Setter Property="Height"     Value="38"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize"   Value="12"/>
        <Setter Property="Margin"     Value="0,0,8,0"/>
    </Style>

    <Style x:Key="DialogButtonAccent" TargetType="Button" BasedOn="{StaticResource AccentButton}">
        <Setter Property="Width"      Value="108"/>
        <Setter Property="Height"     Value="38"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize"   Value="12"/>
        <Setter Property="Margin"     Value="0,0,0,0"/>
    </Style>

    <!-- ============ Toggle switch (CheckBox-based) ============ ▸
         Rectangular "square" track with a small rounded thumb that slides edge-to-edge.
         Label sits slightly higher (TextOptions.TextFormattingMode="Ideal") with a small
         left margin so it doesn't crowd the track. -->
    <Style x:Key="ToggleSwitch" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource App.Text}"/>
        <Setter Property="FontFamily" Value="{StaticResource App.FontFamily}"/>
        <Setter Property="FontSize"   Value="{StaticResource App.FontSize}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor"     Value="Hand"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="TextOptions.TextFormattingMode" Value="Ideal"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" Background="Transparent">
                        <Border x:Name="track"
                                Width="38" Height="20"
                                CornerRadius="3"
                                Background="{StaticResource App.BorderStrong}"
                                BorderBrush="{StaticResource App.Border}"
                                BorderThickness="1"
                                Margin="0,0,10,0"
                                VerticalAlignment="Center"
                                SnapsToDevicePixels="True">
                            <!-- Thumb stays vertically centered and slides between ends so it never
                                 looks misaligned mid-toggle. Rounded rectangle for the new square track. -->
                            <Border x:Name="thumb"
                                    Width="14" Height="14"
                                    CornerRadius="2"
                                    Background="#FFFFFF"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Margin="2,0,0,0"
                                    RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <TranslateTransform x:Name="thumbShift" X="0"/>
                                </Border.RenderTransform>
                            </Border>
                        </Border>
                        <ContentPresenter VerticalAlignment="Center" RecognizesAccessKey="True"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="track" Property="Background" Value="{StaticResource App.Accent}"/>
                            <Setter TargetName="track" Property="BorderBrush" Value="{StaticResource App.Accent}"/>
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="thumbShift" Storyboard.TargetProperty="X"
                                                         To="18" Duration="0:0:0.14"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="thumbShift" Storyboard.TargetProperty="X"
                                                         To="0" Duration="0:0:0.14"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="track" Property="BorderBrush" Value="{StaticResource App.Accent}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource App.DisabledText}"/>
                            <Setter TargetName="track" Property="Background" Value="{StaticResource App.Disabled}"/>
                            <Setter TargetName="thumb" Property="Background" Value="#888888"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============ Context menu ============ -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background"      Value="{StaticResource App.PanelBgHi}"/>
        <Setter Property="BorderBrush"     Value="{StaticResource App.Accent}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground"      Value="{StaticResource App.Text}"/>
        <Setter Property="FontFamily"      Value="{StaticResource App.FontFamily}"/>
        <Setter Property="FontSize"        Value="{StaticResource App.FontSize}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8" Padding="5">
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource App.Text}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding"    Value="12,8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="5"
                            Padding="{TemplateBinding Padding}" MinHeight="30">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="26"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter Grid.Column="0" x:Name="ico" ContentSource="Icon"
                                              VerticalAlignment="Center" HorizontalAlignment="Center"
                                              TextElement.Foreground="{StaticResource App.Accent}"/>
                            <ContentPresenter Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True"
                                              VerticalAlignment="Center" Margin="4,0,0,0"/>
                            <TextBlock Grid.Column="2" x:Name="gesture" Text="{TemplateBinding InputGestureText}"
                                       Foreground="{StaticResource App.TextDim}" Margin="16,0,0,0"
                                       VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" x:Name="arrow" Text="&#xE76C;" FontFamily="Segoe MDL2 Assets"
                                       FontSize="11" Foreground="{StaticResource App.TextDim}" Margin="16,0,0,0"
                                       VerticalAlignment="Center" Visibility="Collapsed"/>
                            <Popup x:Name="PART_Popup" AllowsTransparency="True" Placement="Right" HorizontalOffset="-2"
                                   IsOpen="{TemplateBinding IsSubmenuOpen}" Focusable="False" PopupAnimation="Fade">
                                <Border Background="{StaticResource App.PanelBgHi}" BorderBrush="{StaticResource App.Accent}"
                                        BorderThickness="1" CornerRadius="6" Padding="4" Margin="0,2,8,8">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.5" Color="#000000"/>
                                    </Border.Effect>
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="arrow"   Property="Visibility" Value="Visible"/>
                            <Setter TargetName="gesture" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Role" Value="TopLevelHeader">
                            <Setter TargetName="arrow"   Property="Visibility" Value="Visible"/>
                            <Setter TargetName="gesture" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource App.AccentSoft}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource App.DisabledText}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Separator" x:Key="MenuSeparator">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Height="1" Background="#5A6273" Margin="6,4"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
